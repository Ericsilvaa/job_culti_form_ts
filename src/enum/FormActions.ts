/* 
  ? Enums
    * 1. Por padrão um enum é baseado em números, começando no zero e incrementa + 1 a cada opção.
    * 2. Facilita o manuseio de grupos de [constantes] nomeadas.
*/
export enum FormActions {
  setCurrentStep,
  setName,
  setLevel,
  setEmail,
  setGithub,
}